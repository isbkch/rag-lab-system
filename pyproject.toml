[project]
name = "failure-lab"
version = "0.1.0"
description = "Local failure laboratory with FastAPI, React, queues, and observable dependencies"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = []
