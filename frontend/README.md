# Failure Lab Frontend

React/Vite dashboard for the local failure laboratory.

```bash
npm install
npm run dev
npm run build
npm run lint
```

The app calls the backend at `VITE_API_BASE_URL` and defaults to
`http://localhost:8000` for local development.
